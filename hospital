#include <iostream>
#include <string>
using namespace std;

	struct Patient
{
	char name[50];
	int age;
	char gender;
};

void create(Patient *);
void remove();
void edit();
void search();
void appointment();



const int SIZE = 50;
	
int main ()
{
	Patient *patientData;
	
	int selection = 1;
	
	cout << "----------------------------------\n";
	cout << "Welcome to Hospital Registration! \n";
	
	
	
	while(selection >= 1 && selection <= 5)
	{
		cout << "----------------------------------\n";
		cout << "How can we help you? \n";
		cout << "Please select from the below section : \n";
		cout << "(1) Create a new patient data \n";
		cout << "(2) Remove existing patient data\n";
		cout << "(3) Edit existing patient data\n";
		cout << "(4) Search for patient\n";
		cout << "(5) Appoint a patient to doctor" << endl;
		cout << "(0) To exit the system" << endl;
		cout << "----------------------------------\n";
		cin >> selection;
		
		
		if(selection == 1)
		{
			create(patientData);
			system("cls");
		}
		else if (selection == 2)
		{
			remove();
			system("cls");
		}
		else if (selection == 3)
		{
			edit();
			system("cls");
		}
		else if (selection == 4)
		{
			search();
			system("cls");
		}
		else
		appointment();
		system("cls");
	}
	cout << "Thanks for using the service";
	return 0;
}

void create(Patient *patientData)
{
	int i ;
	char key;
	
	system("cls");
	cout << "************************************" << endl;
	cout << "****Patient Data Registry System****\n";
	cout << "************************************" << endl << endl;
	
	while ( key != 'n' && key != 'N')
	{
	
	Patient patientData;

	cout << "Please Enter Patient Name : ";
	cin>> patientData.	name;
	cout << "Continue to add patient? (y/n) : ";
	cin >> key;
	cout << endl;
}

}
void remove()
{
	cout << "pick 2";
}
void edit()
{
	cout << "pick 3";
}
void search()
{
	cout << "pick 4";
}
void appointment()
{
	cout << "pick 5";
}
